window.app = {
    // folder to load views from.
    viewsFolder: "views",
    // default view to load
    defaultView: "home",
    // id of element to host layouts
    layoutContainer: "main",
    // default layout to load
    defaultLayout: "layout",
    // id of element to host layout content.
    contentContainer: "content",
    // view to load on error
    errorView: "views/error/error"
}